# POCstars-firmware-dump-script
DUMP THE FIRMWARE ON PUSH TO TALK RADIOS LIKE, TRINIDAD, BEOFANG, AND ALLTHEWAY

The CLI and GUI now probe common Android block-device locations (`/dev/block/by-name` and `/dev/block/bootdevice/by-name`) and only dump partitions that are actually present on the connected device.
